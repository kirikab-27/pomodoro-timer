{
  "buildCommand": "npm run build",
  "framework": "nextjs",
  "installCommand": "npm install",
  "env": {
    "DATABASE_URL": "file:./dev.db"
  },
  "build": {
    "env": {
      "DATABASE_URL": "file:./dev.db"
    }
  }
}